apiVersion: v1
kind: ServiceAccount
metadata:
  name: price-insight-ksa
  namespace: price-insight
  annotations:
    # Workload Identity: bind to the GCP service account that has Cloud SQL Client role
    # Replace PROJECT_ID and GCP_SA_NAME with your values
    iam.gke.io/gcp-service-account: price-insight-backend@acme-pricewatch.iam.gserviceaccount.com
