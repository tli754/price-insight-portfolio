# Backend origin — proxied for /api/** and /auth/** route rules.
# Auth itself lives entirely in the backend (single shared password -> JWT in
# an httpOnly cookie); the frontend has no session/OAuth config of its own.
NUXT_BACKEND_URL=http://localhost:4000
